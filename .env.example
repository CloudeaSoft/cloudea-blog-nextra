NEXT_PUBLIC_BASE_URL = "http://localhost:3000"
# Site root: leave unset, empty, or "/". Subpath example: "/blog"
# Cloudflare Build Variables cannot set an empty value — omit this var or set "/".
NEXT_PUBLIC_BASE_PATH = ""
