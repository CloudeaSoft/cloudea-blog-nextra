"use client";

import type { PageMapItem } from "nextra";
import type { FC } from "react";

/** Placeholder sidebar; not currently wired into the layout. */
export const Sidebar: FC<{ pageMap: PageMapItem[] }> = () => {
	return null;
};
