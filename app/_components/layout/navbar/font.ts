import { M_PLUS_Rounded_1c } from "next/font/google";

/** Soft rounded face shared with the loading screen — fits the sakura / leaf brand. */
export const navbarFont = M_PLUS_Rounded_1c({
	subsets: ["latin"],
	weight: ["500", "700"],
});
